\begin{halfpage}
\question{5}{¿Es posible replicar el comportamiento de una estructura \texttt{if}-\texttt{elif}-\texttt{else} usando únicamente instrucciones \texttt{if} (sin \texttt{elif} ni \texttt{else})? Si sí se puede, muestre cómo; si no, explique por qué.

\textit{Consejo: Sea breve. No dedique más de 7 minutos a este punto.}
}
\end{halfpage}