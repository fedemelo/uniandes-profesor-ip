\newpage

\namedgridquestion{16}{Escriba una función que recibe como parámetro una cantidad de bytes (entero). La función debe convertir esa cantidad de bytes en gigabytes (GB), megabytes (MB), kilobytes (KB) y bytes (B). Luego, debe retornar una cadena de caracteres en el formato

\mintinline{python}|"El tamaño es de x GB, y MB, z KB y w B"|

No tiene que saber qué es un byte. Solo que: 1024 bytes equivalen a 1 KB, 1024 KB forman 1 MB y 1024 MB son iguales a 1 GB.

Considere que esta función pertenece a la capa de lógica; ergo, no interactúa con el usuario.

\textit{Consejo: No demore más de 30 minutos en este punto.}}
